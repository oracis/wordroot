# 词源划词 WordRoot — 隐私权说明

最后更新：2026-08-31
项目地址：https://github.com/oracis/wordroot

---

## 核心原则

**本扩展不收集、不上传、不销售任何用户个人数据。** 全部数据（设置、生词本、缓存、统计）仅保存在你本机的 `chrome.storage.local` 中。**任何代码路径都不会把数据发送到我们的服务器**——本项目不运营任何后端。

下面逐条说明每个权限和外部请求的具体用途与数据流向。

---

## 一、权限用途（Chrome 商店必填）

| 权限 | 用途 | 涉及数据 | 是否离开本机 |
|---|---|---|---|
| `storage` | 保存设置项（API Key、模型、朗读源等）、生词本、查词缓存、使用统计 | 本机配置与用户内容 | ❌ 全部在 `chrome.storage.local` |
| `tts` | 调用浏览器内置 `chrome.tts` 朗读英文单词 | 查词文本 | ❌ 浏览器本地语音引擎 |
| `declarativeNetRequest` | 把浏览器中点开的 `.pdf` 链接自动重定向到本扩展内置的 pdf.js 阅读器，从而在 PDF 上也能划词 | 目标 PDF 的 URL | ❌ 仅在浏览器进程内改路由 |
| `<all_urls>` (host) | ① content script 注入到所有网页以实现划词查词；② 支持 PDF 自动接管；③ 用户配 LLM Key 后访问其自有 API 域名；④ 高级「自建 TTS 中转」请求 localhost/127.0.0.1 中转服务（已包含在 `<all_urls>` 中） | 划词文本、目标 URL、本地中转 URL | 见下表 |

### 关于 `<all_urls>` 的 Limited Use Justification

- **使用场景**：content script 必须在用户访问的任意英文网页上监听划词行为（这是本扩展的核心功能），同时 DNR 规则匹配 `*.pdf` 链接也需要全 URL 模式。
- **最小化**：我们没有用 `<all_urls>` 收集浏览历史、注入广告、跨站跟踪或做行为画像。仅注入一个 content script 用于划词事件监听。
- **用户可控**：在 `chrome://extensions` → 扩展详情中可一键关闭"在所有网站上访问站点数据"，关闭后仅在用户主动点击图标查词时仍可用。

---

## 二、外部网络请求（用户可见）

| 域名 | 触发条件 | 传输内容 | 必要性 |
|---|---|---|---|
| `https://dict.youdao.com/dictvoice` 与 `/jsonapi` | 用户在网页/PDF/EPUB 中划词，且离线词库未命中 | 单词本身 | 默认发音+释义来源（无 Key） |
| `https://api.openai.com/v1`（或用户自定义 baseURL） | 用户在选项页填入自己的 LLM Key 后，发起"AI 词源故事"查询 | 单词本身、API Key | 用户主动配置才生效，Key 仅在请求头直发 |
| `http://localhost:8787` 或用户自定义中转 | 用户在选项页选择"自建中转"并保存 | 单词、User-Agent | 高级功能，需用户先主动部署 `tts-relay/server.py` |

**没有任何"分析/统计/上报"请求。** 本扩展不上报任何遥测数据到我们这里，也不嵌入任何第三方分析 SDK。

### AI 词源（OpenAI 兼容）数据流

- 请求只在用户配置了 API Key 后才会发起；
- Key 存在本机 `chrome.storage.local`，不加密（浏览器扩展的标准存储方式，与其他扩展一致）；
- 每次查词发送 1 条用户消息（用户划中的单词），1 条 system 消息（内置 prompt，要求结构化 JSON 输出）；
- 响应结果按"单词 → 词源 JSON"的 key 缓存在本机，下次查同一个词不会再发请求；
- 用户随时可在选项页删除 API Key，立即停止所有外部 AI 请求。

---

## 三、本地存储数据清单

`chrome.storage.local` 持久化以下字段：

| 字段 | 内容 | 清除方式 |
|---|---|---|
| `apiKey` / `baseURL` / `model` | LLM 配置 | 选项页改写或手动 `chrome://extensions` 清空存储 |
| `ttsMode` / `ttsRelay` | 朗读源设置 | 同上 |
| `autoPdf` | 是否自动接管 PDF 链接 | 选项页复选框 |
| `wr_vocab` | 生词本（含用户收藏的单词与对应词源快照） | 生词本页"清空"或 `chrome.storage.local` 删除 |
| `wr_cache` | 查词结果缓存（避免重复请求） | 选项页 → 开发者模式 → 清空统计（连同其他用量数据一起） |
| `wr_usage` / `wr_usage_hist` | 当日/历史使用统计（查词次数、AI 次数、PDF/EPUB 打开次数、收藏次数、付费闸门触达次数） | 选项页 → 开发者模式 → 清空统计 |
| `wr_devMode` | 开发者模式开关（仅影响本浏览器 profile，不影响他人） | 退出开发者模式或清存储 |

**没有用户身份标识、没有设备 ID、没有跨浏览器同步、没有云端账号绑定。** 换台电脑或重装浏览器 = 重新开始。

---

## 四、与本扩展开发者的数据共享

**零。** 本扩展没有运营任何服务器、没有用户账号系统、没有 email 收集表单、没有远程组件。

你看不到任何其他用户的数据；你只能看到你本机这一份。扩展作者想要解真实使用量，必须依赖：

1. Chrome 商店后台的"Weekly Users"统计（Google 官方提供，匿名聚合，不下钻到个人）；
2. 商店评论区与应用商店站内信。

---

## 五、开源与可审计

- 全部源码公开在 https://github.com/oracis/wordroot
- 关键模块（查词链路、缓存、统计、付费闸门）附有单元/集成测试在 `tests/`
- 任何第三方可在 30 秒内完成"全扩展是否包含上报代码"的审计：当前 `git grep` 任何 `fetch(|XMLHttpRequest(|sendBeacon(` 命中均为下表三条，且都直接对用户可见的第三方域名

---

## 六、用户控制

| 行为 | 路径 |
|---|---|
| 关闭/卸载扩展 | `chrome://extensions` |
| 拒绝 <all_urls> host 权限 | 扩展详情 → "在所有网站上访问站点数据" → 关闭 |
| 拒绝 localhost 权限 | 选项页"自建中转"模式保存时弹窗 → 拒绝 |
| 删除 API Key | 选项页 → 清空 API Key 字段 → 保存 |
| 关闭 PDF 自动接管 | 选项页 → 取消勾选"自动接管 PDF 链接" → 保存 |
| 清空生词本 | 生词本页 → 清空 |
| 清空本机全部数据 | 扩展详情 → "清除存储" |

---

## 七、政策变更

本说明的更新会随版本号在 GitHub release 中标注。如发生实质性变更（增加新的外部请求、新增可识别字段、新增远端通信），会提前在更新日志中显著公告。

## 八、联系

隐私相关问题：GitHub Issues（https://github.com/oracis/wordroot/issues）。
